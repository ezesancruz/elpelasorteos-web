Qué quiero

“Agregá en el panel de edición de la tarjeta detalleVisual un botón/interruptor que invierta el layout: si hoy se ve imagen izquierda + texto derecha, al activarlo se vea texto izquierda + imagen derecha. En mobile debe seguir apilado (imagen arriba, texto abajo) y solo invertir en md+.”

Cambios mínimos (especificación técnica)

Schema / contenido

Añadir un booleano en la tarjeta: reverse: boolean (default: false).

{
  "id": "numero-comprobante",
  "type": "detalleVisual",
  "title": "🍀 Tu número de la suerte",
  "description": "...",
  "image": "/uploads/....jpg",
  "reverse": false
}


UI del editor (sidebar)

Agregar un control tipo Toggle o botón con estado:

Label: “Invertir orden (imagen/texto)”

Al hacer click: card.reverse = !card.reverse y guardar.

Ejemplo (React):

<button
  className={`w-full rounded-md px-3 py-2 text-sm font-medium 
              ${card.reverse ? 'bg-amber-500 text-black' : 'bg-white/10 text-white'}`}
  onClick={() => updateCard(card.id, { reverse: !card.reverse })}
>
  {card.reverse ? 'Orden invertido' : 'Invertir orden (imagen/texto)'}
</button>


Render del componente (Hero/Tarjeta detalleVisual)

Usar flex con row / row-reverse solo desde md::

<div className="rounded-3xl p-4 md:p-6 bg-black/30">
  <div className={`flex flex-col md:flex-row ${card.reverse ? 'md:flex-row-reverse' : ''} gap-6 md:gap-8 items-center`}>
    {/* Bloque Imagen */}
    <div className="w-full md:w-1/2">
      <div className="overflow-hidden rounded-2xl">
        <img src={card.image} alt={card.title} className="w-full h-48 sm:h-56 md:h-64 object-cover" />
      </div>
    </div>

    {/* Bloque Texto */}
    <div className="w-full md:w-1/2">
      <h3 className="text-2xl md:text-3xl font-extrabold text-amber-300">{card.title}</h3>
      <p className="mt-3 text-white/90 leading-relaxed">{card.description}</p>
    </div>
  </div>
</div>


Alternativa sin row-reverse: controlar con utilidades order-*:

<div className="flex flex-col md:flex-row gap-6 md:gap-8 items-center">
  <div className={`w-full md:w-1/2 ${card.reverse ? 'md:order-2' : 'md:order-1'}`}>…Imagen…</div>
  <div className={`w-full md:w-1/2 ${card.reverse ? 'md:order-1' : 'md:order-2'}`}>…Texto…</div>
</div>


Persistencia

Asegurate de que updateCard actualice el JSON (site-content.json) y dispare live preview.

Accesibilidad y responsive

En mobile se mantiene flex-col (no cambia).

alt en la imagen.

El toggle debe ser accesible: aria-pressed={card.reverse}.

Migración

Si el campo no existe en tarjetas viejas, asumir reverse = false.